export enum CookieType {
  NECESSARY = 'necessary',
  FUNCTIONAL = 'functional',
  PERFORMANCE = 'performance',
  MARKETING = 'marketing'
}

export enum CookieOptions {
  ALLOW = 'allow',
  DENY = 'deny'
}
